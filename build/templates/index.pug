doctype html
html(lang="en")
  include head
  body
    marquee(class="stickman-container" direction="right" scrollamount="3")
      img(class="stickman" src='/img/transparent.gif')
    div(class="countdown")
      img(class="pic" src='/img/muschel.svg')
      h2(id="countdown" class="counter")
    div(class="main-page")
      if page == "gallery"
        include gallery
      else if page == "carousel"
        include carousel
        each activity, index in stravaData
          p= activity
  include footer
